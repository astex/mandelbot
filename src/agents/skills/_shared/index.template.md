# <project name>

**Plan:** <absolute path to ~/.claude/plans/...>

## How we work

### Protocol

<short protocol note for this batch of children — roles, what to escalate, anything flow-specific. At minimum: read the governing plan, start implementing immediately, use the block/unblock handshake if stuck, close tabs when done. Add anything flow-specific: file-ownership boundaries, branching conventions, etc.>

### Reviews

<default: reviews are human-in-the-loop — once your PR is open, set `awaiting_review` instead of `done` and stay alive for review feedback. Only override if this project explicitly wants autonomous merging — e.g. "Reviews are autonomous: children close on `done` without human review." See the awaiting_review section in `_shared/coord.md`.>

## Children

- **<label>** (<role>) — [link](./<label>.coord.md)
